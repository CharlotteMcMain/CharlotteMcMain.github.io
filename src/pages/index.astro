---
import { getCollection } from "astro:content";
import SiteLayout from "../layouts/SiteLayout.astro";

const posts = await getCollection("posts");
---

<SiteLayout title="Charlotte McMain Physics" description="A-Level & GCSE resources">
  <main class="home">
    <h1>Welcome 👋</h1>
    <p>Pick a recent post:</p>

    <ul>
      {posts.map((post) => (
        <li>
          <a href={`/${post.slug}`}>{post.data.title}</a>
        </li>
      ))}
    </ul>
  </main>
</SiteLayout>

<style>
  .home {
    max-width: 42rem;
    margin: 2rem auto;
    padding: 0 1rem;
  }
</style>
